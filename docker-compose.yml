version: "3.8"

services:
  postgres:
    image: postgres:17
    container_name: pg_demo
    environment:
      POSTGRES_DB: demo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
    ports:
      - "5432:5432"